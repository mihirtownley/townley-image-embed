services:
  - type: web
    name: townley-image-embed
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --timeout 120
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0